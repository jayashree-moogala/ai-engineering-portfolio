# learning-journal

## Overview

> Concepts learnt while learning on the project.

## Concepts, questions, findings

Why does RAG sometimes hallucinate?

If the retriever returns low-quality or distant matches, the LLM receives poor context. We can improve accuracy by using better chunking, stronger embedding models, metadata filtering, or applying a similarity threshold before passing retrieved chunks to the model.


